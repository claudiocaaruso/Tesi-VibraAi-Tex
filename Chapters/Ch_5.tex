\chapter{Results and discussion}
\label{ch:results}

This chapter presents the experimental results obtained with the computational pipeline previously defined. 
\section{Exploratory PCA of the dataset}
\label{sec:pca_dataset}

In this work, PCA was first used as an exploratory tool to inspect the structure of the Raman dataset before building the classification model. To perform this step, PCA was calculated on the entire dataset after removing the non-informative labels, namely unlabelled/transparent pixels and artefacts, as described in \Cref{sec:labels-ch4}.

As discussed previously, PCA is an unsupervised machine learning method. The final objective of this analysis is therefore not to decide which spectral components are more relevant to distinguish different biological samples or to classify them according to their composition, but rather to investigate the content of the spectra in terms of explained variance to understand whether they contain particularly relevant directions and whether a more compact representation may be adequate for our dataset. If this were the case, it would be possible to reduce the dimensionality of our data to the number of components most suitable for training the artificial neural network, for the reasons explained in \Cref{sec:dimensionality_reduction}. 


\subsection{Spectral input and centering}
\label{subsec:pca_input_centering}

The data was oranized in matrix \(\mathbf{X}\) introduced in
\Cref{sec:dimensionality_reduction}.
The matrix was constructed so that each column corresponds to one of the 483 bands and each row contains the intensities of each spectrum.

\Cref{fig:original_centered_spectrum} represents the centering of one single spectrum as described in \Cref{subsec:pca_centering_covariance}. This step is applied to all spectra in the dataset, and the new principal directions are calculated on the centered matrix \(\mathbf{X}_c\).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{Images/images_ch_3/05_original_vs_centered_spectrum.png}
    \caption{Example of an original Raman spectrum and the corresponding centered spectrum used for PCA.}
    \label{fig:original_centered_spectrum}
\end{figure}

\subsection{Explained variance of the principal components}
\label{subsec:pca_explained_variance_vibra}

After the computation of the new principal components, the most interesting parameter that has been evaluated is the percentage of variance explained by each of these components, related to the singular values as explained in \Cref{subsec:pca_svd}. The scree plot in 
\Cref{fig:pca_scree_plot} shows how most of the variance is retained by the first components, as expected from the theory, while the contribution of later components rapidly becomes smaller.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{Images/images_ch_3/07_scree_plot_explained_variance.png}
    \caption{Explained variance associated with the first principal components.}
    \label{fig:pca_scree_plot}
\end{figure}

The cumulative explained variance illustrated in \Cref{fig:pca_cumulative_variance} shows a steep initial increase and a progressive flattening of the curve. This proves how the last components are significantly less relevant considering the amount of information they contain; more specifically, we observe that keeping a representation made by the first 89 components only allows us to reach a 95 percent explained variance threshold.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{Images/images_ch_3/06_cumulative_explained_variance.png}
    \caption{Cumulative explained variance as a function of the number of retained principal components.}
    \label{fig:pca_cumulative_variance}
\end{figure}

\Cref{fig:pca_reconstruction_comparison} shows the effect of reconstructing the same
spectrum with just one component and with \(89\) components. The first component is able to capture the dominant trend, whereas the reconstruction using \(89\) components preserves the main spectral profile while providing a more compact representation; for this reason, it is of particular interest.

\begin{figure}[H]
    \centering
    \subfloat[Reconstruction using the first principal component.\label{fig:pca_reconstructed_1}]{
        \includegraphics[width=0.48\textwidth]{Images/images_ch_3/12_original_vs_reconstructed_1_components.png}
    }
    \hfill
    \subfloat[Reconstruction using the first \(89\) principal components.\label{fig:pca_reconstructed_89}]{
        \includegraphics[width=0.48\textwidth]{Images/images_ch_3/12_original_vs_reconstructed_89_components.png}
    }
    \caption{Comparison between an original Raman spectrum and its PCA reconstruction using different numbers of retained principal components.}
    \label{fig:pca_reconstruction_comparison}
\end{figure}

\subsection{Projection of spectra in PCA space}
\label{subsec:pca_projection_vibra}

In the PCA space, each spectrum can be visualised as a point in a 483-dimensional space if all principal directions are considered, and the Raman dataset is therefore a point cloud.  Reducing the representation to fewer components also reduces the dimensionality of this space, projecting the data onto the directions along which the data cloud extends the most, which are equivalent to the first principal directions. 

The score plots in \Cref{fig:pca_score_pc1_pc2} and \Cref{fig:pca_score_pc1_pc483} were generated on a random subset of
\(10000\) spectra.

\begin{figure}[H]
    \centering
    \subfloat[Projection onto PC1 and PC2.\label{fig:pca_score_pc1_pc2}]{
        \includegraphics[width=0.48\textwidth]{Images/images_ch_3/08_pca_score_plot_pc1_pc2.png}
    }
    \hfill
    \subfloat[Projection onto PC1 and PC483.\label{fig:pca_score_pc1_pc483}]{
        \includegraphics[width=0.48\textwidth]{Images/images_ch_3/09_pca_score_plot_pc1_pc483.png}
    }
    \caption{Score plots of Raman spectra in the PCA space.}
    \label{fig:pca_score_plots}
\end{figure}

\Cref{fig:pca_score_pc1_pc2} shows how the cloud is distributed along both the PC1 and PC2 axes, with a wider spatial extent along the former, consistent with the fact that the former component explains a greater variance. Points associated with spectra labelled differently, depicted in different colours, overlap, indicating that the biological classes are not distinguishable in this examination. More specifically, it is relevant to point the attention on the distribution of the tumoral classes: label 2 (\textit{tumor}) appears widely distributed, suggesting a strong spectral similarity to several of the tissues under consideration; label 20 (\textit{tumor with necrosis}) seems to be concentrated more on the right side of the cloud, but it still overlaps too much with other labels to allow for definitive conclusions. Since PCA is unsupervised, it is not necessarily expected to find a criterion for classification
 
\Cref{fig:pca_score_pc1_pc483} highlights the large difference in the distribution of data along the first and last components calculated by PCA. The cloud is particularly wide along the first component and compressed almost to zero along PC483, with few exceptions with respect to the number of data. 

\section{Configuration of the final experiments}
\label{sec:final_experiments_configuration}

The final ANN models were trained for the two binary tasks reported in \Cref{tab:final_binary_experiments}.
\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{0.28\textwidth}|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|}
        \hline
        \textbf{Experiment} & \textbf{Positive class} & \textbf{Negative class} \\
        \hline
        \begin{tabular}[t]{@{}c@{}}Tumour-related class\\vs\\non-tumour\end{tabular} & \begin{tabular}[t]{@{}c@{}}Tumour (2)\\Tumour with necrosis (20)\end{tabular} & \begin{tabular}[t]{@{}c@{}}Valid non-tumour classes\\(0, 3, 4, 5, 6, 8, 10, 19, 23)\end{tabular} \\
        \hline
        \begin{tabular}[t]{@{}c@{}}Tumour-related class\\vs\\stroma\end{tabular} & \begin{tabular}[t]{@{}c@{}}Tumour (2)\\Tumour with necrosis (20)\end{tabular} & Tumour stroma (6) \\
        \hline
    \end{tabularx}
    \renewcommand{\arraystretch}{1}
    \caption{Binary classification experiments evaluated in the final analysis.}
    \label{tab:final_binary_experiments}
\end{table}

In both experiments, the positive class includes tumour and tumour with necrosis, consistently with the definition of the binary classification problems in \Cref{sec:binary_classification-ch4}. The first experiment compares this tumour-related class with tumour stroma, whereas the second compares it with all valid non-tumour tissue classes available in the dataset. The latter setting is broader and more heterogeneous, since its negative class includes multiple histopathological labels.

The final ANN experiments are evaluated in terms of training behaviour, aggregate performance, per-sample variability and the effect of spatial post-processing.

\section{Hyperparameter selection}
\label{sec:hyperparameter_selection}

%check if the title is good or it must be changed

The configuration adopted for the final classification experiments was selected through the hyperparameter search described in \Cref{sec:training-ch4}, which compared different model sizes, numbers of retained PCA components and batch sizes.

The comparison of the tested configurations showed comparable results for all three architectures at equal number of retained PCs. A substantial improvement in the metrics was registered when raising the number of PCs from 1 to 20 for the macro-class problem (\textit{tumor} vs. \textit{healthy}), and a plateau from 20 to 483 PCs. In the single-class problem (\textit{tumor} vs. \textit{tumor stroma}), the metrics improved from 1 to 10, flattening out for bigger dimensionalities, as shown in \cref{fig:heatmap_auc_macroclass,fig:heatmap_auc_singleclass} respectively.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Images/images_ch_5/heatmap_auc_macroclass.png}
    \caption{AUC across the grid of network architectures and number of retained PCs components for the macro-class problem.  AUC stabilizes for numbers of PCs greater than 20.(\textit{tumor} vs. \textit{healthy}).}
    \label{fig:heatmap_auc_macroclass}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Images/images_ch_5/heatmap_auc_singleclass.png}
    \caption{AUC across the grid of network architectures and number of retained PCs components for the single-class problem. AUC stabilizes for numbers of PCs greater than 10.(\textit{tumor} vs. \textit{tumor stroma}).}
    \label{fig:heatmap_auc_singleclass}
\end{figure}

Although some configurations with more components or larger networks obtained equally good scores for AUC, their training curves showed less stable behaviour and a larger discrepancy between training and validation performance which also increased with the epochs. These behavior, exemplified by \cref{fig:overfitting_example_L200} is a clear sign of overfitting. The small model, on the contrary, showed similar behaviors in training and validation for all the considered metrics (\cref{sec:aggregated_classification_results}) and was therefore prefered as a final choice.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/images_ch_5/overfitting_example_L200.png}
    \caption{Training and validation curves for the large architecture (L) with \(200\) retained PCA components on the macro-class problem (\textit{tumor} vs. \textit{healthy}). The lines show the average value of the metric across 5 folds, while shaded areas display $\pm\sigma$ from the average. The growing separation between training and validation curves is a sign of overfitting.}
    \label{fig:overfitting_example_L200}
\end{figure}

The maximum number of epochs was set to \(100\), although \textit{early stopping} \cref{sec:training-ch4} often halted the training as soon as at epoch 20 due to validation already plateauing. \textit{Early stopping} often acted when prematurely due to noise in the validation curves when working with small batch sizes smaller than 2048. Increasing batch size does in fact average the gradient descent step over more observations, smoothing out the curve and speeding up training, although raising the risk of converging towards sharp minima, which would lead to poor generalization. The final value for the batch size was set as 4096, found to be a good compromise for this task.

%talk about computational complexity and training times

The decision threshold used to convert the predicted probability into a binary label was selected empirically by comparing threshold values between \(0.45\) and \(0.50\). The final value was set to \(\tau=0.49\), slightly below the standard threshold of \(0.50\), as it provided a compromise between favouring recall for the positive class and maintaining balanced overall performance. In this application, a false negative, corresponding to a tumour-related spectrum classified as non-tumour, is considered more critical than a false positive, corresponding to a non-tumour spectrum classified as tumour-related. A slightly lower threshold increases the tendency of the classifier to assign spectra to the positive class, supporting recall to tumour-related spectra without excessively penalizing accuracy, as seen in \cref{sec:evaluation_metrics-ch4}.

\begin{table}[H]
    \centering
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Hyperparameter} & \textbf{Macro-class} & \textbf{Single-class} \\
        \hline
        Network architecture & \multicolumn{2}{c|}{Small (S)} \\
        \hline
        Retained PCA components & \(20\) & \(10\) \\
        \hline
        Batch size & \multicolumn{2}{c|}{\(4096\)} \\
        \hline
        Decision threshold & \multicolumn{2}{c|}{\(\tau=0.49\)} \\
        \hline
    \end{tabular}
    \caption{Configurations used for the final ANN experiments, for the macro-class (\textit{tumor} vs. \textit{healthy}) and single-class (\textit{tumor} vs. \textit{tumor stroma}) problems.}
    \label{tab:selected_hyperparameters}
\end{table}


\section{Aggregated classification results}
\label{sec:aggregated_classification_results}

The configurations chosen for the two approaches, listed in \cref{tab:selected_hyperparameters} were further analized to asses their effectiveness.

The confusion matrixes and ROC curves, explained in \cref{sec:evaluation_metrics-ch4}, showed good results for both problems. The macro-class approach performed slightly worse the single-class one, especially in non-tumoral classification (True negatives scores equaled 0.71 and 0.75 respectively). This is a direct consequence of the internal heterogeneity for the healthy class. The ROC curves for showed symmetrical behavior respect to the TPR and FNR, with high variability depending on the validation fold composition. Mean AUC across 5 folds achieved scores of $\text{AUC} = 0.799\pm0.064$, relative standard deviation $\sigma_{relative} = 8\%$, for the macro-class problem (\cref{fig:results_S20_macroclass}), and $\text{AUC} = 0.836\pm0.038$, $\sigma_{relative} = 4.5\%$, for the single-class problem (\cref{fig:results_S10_singleclass}). The difference in mean AUC and standard deviation once again indicates the macro-class approach as the most difficult and inconsistent.


\begin{figure}[H]
    \centering
    \subfloat[Confusion matrix.\label{fig:CM_S20_macroclass}]{
        \includegraphics[width=0.4\textwidth]{Images/images_ch_5/CM_S20_macroclass.png}
    }
    \hfill
    \subfloat[ROC curve.\label{fig:ROC_S20_macroclass}]{
        \includegraphics[width=0.4\textwidth]{Images/images_ch_5/ROC_S20_macroclass.png}
    }
    \caption{Confusion matrix and ROC curve for the macro-class problem (\textit{tumor} vs. \textit{healthy}).}
    \label{fig:results_S20_macroclass}
\end{figure}

\begin{figure}[H]
    \centering
    \subfloat[Confusion matrix.\label{fig:CM_S10_singleclass}]{
        \includegraphics[width=0.4\textwidth]{Images/images_ch_5/CM_S10_singleclass.png}
    }
    \hfill
    \subfloat[ROC curve.\label{fig:ROC_S10_singleclass}]{
        \includegraphics[width=0.4\textwidth]{Images/images_ch_5/ROC_S10_singleclass.png}
    }
    \caption{Confusion matrix and ROC curve for the single-class problem (\textit{tumor} vs. \textit{tumor stroma}).}
    \label{fig:results_S10_singleclass}
\end{figure}


The training behavior was observed to be different between the two problems. The validation curves for the macro-class classification displahed very high variability stabilized almost immediately (\cref{fig:curves_S10_singleclass}). The model learns very quickly the basic patterns and trends succeding to achieve an AUC of 0.799, specially thanks to the homogeneity of the the tumoral class, but does not improve further and fails to generalize as well in for the healthy class.
The curves for the single-class problem, on the other hand, improved regularly in the early stages of training until reaching a plateau after $\sim5$ epochs. All the validation curves sit above the training ones for the first epochs due to being evaluated after the weights are updated (while training is evaluated before the weight update). The two curves then intersect and validation stays below training, within acceptable margin. the random fold composition and small validation set size cause the standard deviation on validation to be greater than on training, and enough to cause the average validation to sit above training for some folds.

\begin{figure}[H]
    \centering
    \subfloat[Macro-class problem (\textit{tumor} vs. \textit{healthy}).\label{fig:curves_S20_macroclass}]{
        \includegraphics[width=0.8\textwidth]{Images/images_ch_5/curves_S20_macroclass.png}
    }
    \\
    \subfloat[Single-class problem (\textit{tumor} vs. \textit{tumor stroma}).\label{fig:curves_S10_singleclass}]{
        \includegraphics[width=0.8\textwidth]{Images/images_ch_5/Curves_S10_singleclass.png}
    }
    \caption{Training and validation curves for the macro-class and single-class problems. The lines show the average value of the metric across 5 folds, while shaded areas display $\pm\sigma$ from the average. The representation is truncated at the number of epochs used to train the fold that stopped first, although other folds took up to 80 epochs to train.}
    \label{fig:training_curves}
\end{figure}


\section{Per-sample analysis}
\label{sec:per_sample_analysis}

\textbf{Writing notes.} This section should move from global pixel-wise performance to the behaviour observed on individual samples.

\begin{itemize}
    \item Report per-sample metrics or rankings, if available.
    \item Identify representative easy and difficult samples.
    \item Comment on whether errors are concentrated in specific samples or distributed across the dataset.
    \item When prediction maps are shown, use them to connect numerical performance with spatial behaviour inside the Raman maps.
    \item Keep the comparison between the two experiments focused on differences that are visible at sample level.
\end{itemize}

\section{Effect of spatial smoothing}
\label{sec:effect_spatial_smoothing}

In this section, the results obtained by applying the spatial filter described in \Cref{sec:spatial_post_processing-ch4} to the model's predicted probabilities are evaluated to verify whether computing a local average can improve the model's predictions and to demonstrate that the spatial context contains crucial information for our purpose.

The comparison of raw and smoothed results for both binary problems, presented in \Cref{tab:smoothing_heatmap}, showed that the filter helps to improve the prediction as expected, but the size of the kernel can't be increased too much and must be chosen with caution based on the total discrimination capacity (\textit{AUC}), the ability to detect the positive class (\textit{TPR}/\textit{recall}) and the ability to preserve the negative one (\textit{TNR}/\textit{specificity}).


\definecolor{pastelblue1}{RGB}{244,248,252}
\definecolor{pastelblue2}{RGB}{229,239,249}
\definecolor{pastelblue3}{RGB}{211,228,244}
\definecolor{pastelblue4}{RGB}{190,215,237}
\definecolor{pastelblue5}{RGB}{168,201,229}

\begin{table}[htbp]
    \centering
    \caption{Result of spatial post-processing with different kernel sizes on \textit{AUC}, \textit{TPR} and \textit{TNR}. VALORI DA CAMBIARE}
    \label{tab:smoothing_heatmap}
    \begin{tabular}{c c c c c c c}
        \hline
        & \multicolumn{3}{c}{\textit{tumour} vs. \textit{tumour stroma}} 
        & \multicolumn{3}{c}{\textit{tumour} vs. \textit{healthy}} \\
        \hline
        Kernel size & \textit{AUC} & \textit{TPR} & \textit{TNR} & \textit{AUC} & \textit{TPR} & \textit{TNR} \\
        \hline
        No smoothing 
        & \cellcolor{pastelblue1}0.8154 & \cellcolor{pastelblue1}0.7623 & \cellcolor{pastelblue3}0.7068
        & \cellcolor{pastelblue1}0.7745 & \cellcolor{pastelblue1}0.7419 & \cellcolor{pastelblue1}0.6688 \\
        
        \(3 \times 3\)
        & \cellcolor{pastelblue3}0.8688 & \cellcolor{pastelblue3}0.8167 & \cellcolor{pastelblue4}0.7343
        & \cellcolor{pastelblue3}0.8269 & \cellcolor{pastelblue3}0.7914 & \cellcolor{pastelblue4}0.7029 \\
        
        \(5 \times 5\)
        & \cellcolor{pastelblue4}0.8797 & \cellcolor{pastelblue3}0.8298 & \cellcolor{pastelblue5}0.7383
        & \cellcolor{pastelblue4}0.8373 & \cellcolor{pastelblue4}0.7997 & \cellcolor{pastelblue5}0.7134 \\
        
        \(10 \times 10\)
        & \cellcolor{pastelblue5}0.8824 & \cellcolor{pastelblue4}0.8423 & \cellcolor{pastelblue3}0.7166
        & \cellcolor{pastelblue5}0.8417 & \cellcolor{pastelblue5}0.8047 & \cellcolor{pastelblue5}0.7114 \\
        
        \(15 \times 15\)
        & \cellcolor{pastelblue4}0.8757 & \cellcolor{pastelblue5}0.8473 & \cellcolor{pastelblue2}0.6888
        & \cellcolor{pastelblue4}0.8376 & \cellcolor{pastelblue5}0.8033 & \cellcolor{pastelblue3}0.6984 \\
        \hline
    \end{tabular}
\end{table}


More specifically, a result of particular merit is attributed to the \(5 \times 5\) filter. This leads to clearly better predictions than smaller filters, in terms of global metrics.
Larger kernels tend to increase \textit{tumour} \textit{recall}, which could be desirable based on what was discussed earlier, but at the same time they lead to an overly aggressive imbalance in the confusion matrix by increasing \textit{false positives} and thus reducing the overall reliability of the classifier. This is why, although the 10x10 filter shows a higher \textit{AUC}, it is not the best compromise when evaluated against the \textit{TNR} and would represent an unwise choice.
The results of applying a too-large filter (\(15 \times 15\)) make it clear that the benefits of smoothing are limited, and that excessively increasing the filter size leads to deterioration, "spreading" the \textit{tumour} class onto the healthy one.

\begin{figure}[H]
    \centering
    \subfloat[\textit{tumour} vs. \textit{tumour stroma}.\label{fig:confusion_matrix_tumour_stroma_smoothed}]{
        \includegraphics[width=0.45\textwidth]{Images/images_ch_5/tumor_troma_smoothed.jpeg}
    }
    \hfill
    \subfloat[\textit{tumour} vs. \textit{healthy}.\label{fig:confusion_matrix_tumour_healthy_smoothed}]{
        \includegraphics[width=0.45\textwidth]{Images/images_ch_5/tumor_healty_smoothed.jpeg}
    }
    \caption{Confusion matrices obtained after spatial post-processing with the \(5 \times 5\) smoothing filter. Compared to the raw results shown in FIGURE? the \textit{TPR} and \textit{TNR} rise.}
    \label{fig:confusion_matrices_smoothed}
\end{figure}

Observing the smoothed prediction maps and comparing them with the raw ones, as shown in \Cref{fig:prediction_maps_raw_smoothed_tumour_stroma}, it is clear that the \(5 \times 5\) filter reduces isolated errors in favour of greater spatial consistency and does not excessively propagate the areas that produced incorrect predictions, leading to the optimal result.

\begin{figure}[H]
    \centering
    \subfloat[Raw prediction map.\label{fig:prediction_map_raw_tumour_stroma}]{
        \includegraphics[width=0.90\textwidth]{Images/images_ch_5/sample_3525_028_map_Map_1_raw.png}
    }
    
    \vspace{0.5em}
    
    \subfloat[Prediction map after \(5 \times 5\) smoothing.\label{fig:prediction_map_smoothed_tumour_stroma}]{
        \includegraphics[width=0.90\textwidth]{Images/images_ch_5/sample_3525_028_map_Map_1_5x5_smoothing.png}
    }
    \caption{Comparison between raw and smoothed prediction maps for the \textit{tumour} vs. \textit{tumour stroma} experiment. FORSE TAGLIARE IL TITOLO POCO LEGGIBILE}
    \label{fig:prediction_maps_raw_smoothed_tumour_stroma}
\end{figure}


\section{Interpretation and comparison with the literature}
\label{sec:interpretation_literature}

\textbf{Writing notes.} This section should interpret the results without turning into a new literature review.

\begin{itemize}
    \item Summarize what the results suggest about Raman spectra as inputs for tumour recognition.
    \item Compare the two experiments in terms of biological and computational difficulty.
    \item Relate the observed performance to the spectral complexity and class heterogeneity discussed in the previous chapters.
    \item Add a short comparison with similar Raman spectroscopy and machine learning studies, if useful.
    \item Avoid overclaiming clinical validity: the results support the feasibility of the pipeline on the available dataset.
\end{itemize}

\section{Limitations of the study}
\label{sec:limitations_study}

Although being based on a large dataset (>1000000 observations), the spectra are strongly spatially correlated. This leads to a substantial reduction in useful independent information. The samples are in fact very few (25), with many types of biological tissue being largely underrepresented. The performance, as seen in \cref{sec:per_sample_analysis}, varies depending on the selected samples. A different dataset split or a larger pool of patients and samples may lead to different results compared to what was found in this thesis, which would be a sign of poor generalization.

Another limitation stems from acquisition and preprocessing. The ANN received spectra which underwent a specific pipeline before being suitable for computational analysis. Different choices for the treatment of the samples, instrumentation, acquisition parameters, or preprocessing steps would reflect on the spectral trends. These differences may transfer to the ANN classification problem, altering the final results. The extent of these changes was not assessed in this thesis, which limits how confidently the pipeline can be expected to generalize to spectra acquired under different conditions.

Spatial smoothing was applied as a post-processing step to improve performance due to common isolated label mismatches. Due to not being an internal part of the ANN decision process, it may also suppress real isolated healthy/tumoral regions as it does with noise. Implementing spatial convolution directly into the ANN could thus lead to more precise classification.

\section{Future developments}
\label{sec:future_developments}

Possible developments of this work could revolve around solving the issues listed in \cref{sec:limitations_study}. This would mean broadening the training pool in terms of both samples and patients, as well as quantifying the influence of different experimental conditions and preprocessing on the final result.

Enlarging the training pool would also increase the number of samples available for the underrepresented biological tissue classes, allowing different binary classifications to be explored. Multiclass classification would be particularly useful, allowing faster, in-vivo and pixel-exact tissue identification, now performed through H\&E staining by a histopathologist.

Further improvements in the deep learning pipeline may be implemented through a convolutional neural network (CNN). Spectral convolution (1D) could lead to better identification of specific peaks and patterns associated with biological signatures. Spatial convolution (2D) on the Raman maps could serve a similar purpose to spatial smoothing, but inside the deep learning pipeline instead of as a heuristic post-processing step.